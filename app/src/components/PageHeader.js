const PageHeader = ({ header }) => (
    <div className="page-header">
        <h2 className="header">
            {header}
        </h2>
        {/* <hr className="divider" /> */}
    </div>
);

export default PageHeader;
