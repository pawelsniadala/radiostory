const HeaderPage = ({ header }) => (
    <div className="header page">
        <h2 className="text">
            {header}
        </h2>
        {/* <hr className="divider" /> */}
    </div>
);

export default HeaderPage;
